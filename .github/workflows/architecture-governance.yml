name: ShareNet Architecture Governance

on:
  push:
  pull_request:

jobs:
  architecture-governance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate frozen architecture and execution graph
        run: python3 tools/architecture_check.py
